<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:title="Select Videos"
        app:titleTextColor="@android:color/white"
        app:menu="@menu/gallery_menu" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_gallery"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="2dp"
            android:clipToPadding="false" />

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone"
            android:id="@+id/error_layout">

            <TextView
                android:id="@+id/tv_error"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Error loading videos"
                android:textColor="@android:color/black"
                android:textSize="16sp"
                android:gravity="center"
                android:layout_marginBottom="16dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_request_permission"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Grant Permission"
                android:visibility="gone" />
        </LinearLayout>

    </FrameLayout>

</LinearLayout>

// Add this to setupObservers() function
setupToolbar()

// Add these new functions to GalleryFragment class

private fun setupToolbar() {
    binding.toolbar.setOnMenuItemClickListener { menuItem ->
        when (menuItem.itemId) {
            R.id.action_select_all -> {
                selectAllVideos()
                true
            }
            R.id.action_import -> {
                importSelectedVideos()
                true
            }
            R.id.action_cancel -> {
                viewModel.clearSelection()
                true
            }
            else -> false
        }
    }
}

private fun selectAllVideos() {
    viewModel.videos.value?.forEach { video ->
        if (!viewModel.isSelected(video)) {
            viewModel.toggleVideoSelection(video)
        }
    }
}

private fun importSelectedVideos() {
    val selectedVideos = viewModel.selectedVideos.value
    if (selectedVideos.isNotEmpty()) {
        // Navigate to editor with selected videos
        // You can use a callback interface here
        val result = Bundle().apply {
            putParcelableArrayList("selected_videos", ArrayList(selectedVideos))
        }
        parentFragmentManager.setFragmentResult("gallery_result", result)
        
        // Clear selection and navigate back
        viewModel.clearSelection()
        parentFragmentManager.popBackStack()
    }
}